game:
  gameId: string
  gameCode: string
  started: boolean
  players: [player]
  spectators: [player]
  turns: [turn]
  deck:
    blackCards: [card]
    whiteCards: [card]

player:
  playerId: string
  name: string
  hand: [card]
  blackCards: [card]

turn:
  turnId: string
  blackCard: card
  playedCards: [[playedCards]]
  winningCards: [card]

playedCards:
  playerId: int
  whiteCards: [card]
card:
  id: int
  text: string
