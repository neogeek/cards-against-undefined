import React from 'react';

import { PageWrapper } from '../components';

export default () => {
    return (
        <PageWrapper>
            <p>Reconnecting to server ...</p>
        </PageWrapper>
    );
};
